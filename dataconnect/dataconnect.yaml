specVersion: "v1"
serviceId: "formula-ai-excel-main"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "formula-ai-excel-main-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
